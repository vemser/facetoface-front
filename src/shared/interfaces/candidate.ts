export interface ICandidate {
    nome: string;
    cidade: string;
    estado: string;
    genero: string;
    email: string;
    observation: string;
    // turma:
    // foto:
    // linguagens:
    // edicao:
  }